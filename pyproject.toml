[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "voice-ai-bot"
version = "0.1.0"
description = "Push-to-talk OpenAI voice daemon for a Raspberry Pi AIY Voice HAT"
requires-python = ">=3.11"
dependencies = [
  "av>=17.0.0",
  "certifi>=2025.0.0",
  "openai>=2.0.0",
  "python-dotenv>=1.0.1",
  "websocket-client>=1.8.0",
]

[project.optional-dependencies]
test = [
  "pytest>=8.0.0",
  "setuptools>=68",
  "wheel",
]

[project.scripts]
voice-ai-bot = "voice_ai_bot.__main__:main"
voice-ai-bot-demo = "voice_ai_bot.desktop_demo:main"
voice-ai-bot-debug-web = "voice_ai_bot.debug_web:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
  "realtime_integration: live OpenAI Realtime API integration tests",
]
